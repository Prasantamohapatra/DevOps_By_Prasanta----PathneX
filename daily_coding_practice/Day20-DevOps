Day 20 — DEBUG DAY
Fix these intentionally broken files:

🔹 Ansible (fixed)
yaml
- name: Install stuff for Prasanta Pathnex
  hosts: all
  become: yes

  tasks:
    - name: Install httpd
      yum:
        name: httpd
        state: present
🔹 Terraform (fixed)
hcl
resource "aws_instance" "PrasantaPathnexEC2" {
  ami           = "ami-123"
  instance_type = "c6i.8xlarge"

  tags = {
    Name = "Prasanta-Pathnex-EC2"
  }
}
🔹 Kubernetes (fixed)
yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prasanta-pathnex-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: prasanta-pathnex
  template:
    metadata:
      labels:
        app: prasanta-pathnex
    spec:
      containers:
        - name: app
          image: nginx
Secrets & Credentials
🔹 Jenkins Pipeline — Using Credentials
I am replicating how to use credentials securely in Jenkins.

groovy
pipeline {
    agent any
    environment {
        INSTITUTE_NAME = "Prasanta Pathnex"
    }
    stages {
        stage('Checkout') {
            steps {
                git url: 'https://github.com/Pathnex/sample-java-app.git'
            }
        }
        stage('Use Secret') {
            steps {
                withCredentials([string(credentialsId: 'PRASANTA_PATHNEX_API_KEY', variable: 'API_KEY')]) {
                    sh 'echo "Using secret $API_KEY for $INSTITUTE_NAME"'
                }
            }
        }
    }
}
🔹 GitLab CI — Using CI/CD Variables
I am replicating how to use GitLab CI variables securely.

yaml
stages:
  - build

variables:
  INSTITUTE_NAME: "Prasanta Pathnex"
  API_KEY: "PLACEHOLDER_SECRET"

build:
  stage: build
  image: alpine:latest
  script:
    - echo "Using secret $API_KEY for $INSTITUTE_NAME"
